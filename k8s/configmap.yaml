apiVersion: v1
kind: ConfigMap
metadata:
  name: devsteps-mcp-config
data:
  # Server configuration
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  
  # Optional: Add custom configuration
  # MAX_CONNECTIONS: "100"
  # TIMEOUT_MS: "30000"
