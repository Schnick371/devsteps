# Bug: Unused chart.js and d3 Dependencies

## Problem
**INCONSISTENT DEPENDENCIES** - package.json declares dependencies that are not imported:

```json
"dependencies": {
  "@schnick371/devsteps-shared": "workspace:*",
  "chart.js": "^4.5.1",  // ❌ NOT IMPORTED
  "d3": "^7.9.0"         // ❌ NOT IMPORTED
}
```

## Evidence
`dashboardPanel.ts` uses **custom Canvas/SVG implementations**:

```typescript
// Line 498-505: Custom Canvas chart (not Chart.js)
// Line 600: Custom SVG graph (not D3.js)
// Line 394: Comment says "Chart.js and D3.js will be bundled" but never used
```

Confirmed by grep search: **NO imports** of chart.js or d3 in extension code.

## Impact
- **Bundle Size**: Unnecessary ~417KB in node_modules (Chart.js 177KB + D3 240KB)
- **Maintenance**: Outdated dependencies in package.json
- **Confusion**: Declared deps don't match actual implementation

## Root Cause
TASK-005 implemented dashboard with custom visualizations instead of Chart.js/D3, but forgot to remove dependencies from package.json.

## Solution
**Option 1: Remove Dependencies** (RECOMMENDED)
```bash
cd packages/extension
pnpm remove chart.js d3
```

**Option 2: Use Real Chart.js/D3** (Future Enhancement)
- Keep dependencies
- Replace custom Canvas/SVG with proper libraries
- Requires esbuild configuration for tree-shaking

## Recommendation
**Remove dependencies** - Custom implementation is lightweight and works well. No need for 417KB overhead.

## Acceptance Criteria
- [ ] chart.js removed from package.json
- [ ] d3 removed from package.json
- [ ] Extension builds successfully
- [ ] Dashboard still renders all charts
- [ ] No TypeScript errors

## References
- Internet research: Chart.js (177KB) + D3 (240KB) bundle size
- dashboardPanel.ts:394 comment "will be bundled separately" never executed
- TASK-005: Dashboard implementation with custom visualizations
