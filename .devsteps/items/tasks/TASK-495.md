Implementiert 3 fehlende shared-Funktionen als atomische Operationen (read → mutate → writeDocsMap): `upsertDocsMapNode` (insert-or-update, nutzt `nextArchId` bei missing ID), `removeDocsMapNode` (entfernt Node + Kinder, verhindert orphaned Subtree), `patchDocsMapNodeItems` (updated `devsteps_items[]` auf einem bestehenden Node). Fügt alle 3 zu `core/index.ts` Export-Block hinzu. Je 1 Unit-Test pro Funktion.