Correctness requirement per ADR-S2-05 — NOT optional UX polish. Design invariant: "At any odd level N ≥ 3, a non-interactive breadcrumb showing all hidden parent headings (H1 through H⌈N/2⌉-1) MUST be visible. Absence of a breadcrumb at Level 3, 5, or any higher odd level is a DEFECT." Implementation: when filterHeadingsByLevel(3) hides H1, inject <div class="devsteps-breadcrumb" role="navigation">▶ {H1-text} ›</div> immediately before each visible H2. When Level 5 hides H1+H2, inject both H1 and H2 breadcrumb chain. CLI format: > **▶ H1-text** › **H2-text** blockquote. Phase C. Depends on STORY-227 (depthSlider.ts).