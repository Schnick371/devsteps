When `devsteps add --type doc` is called (or future `devsteps add --type adr`), auto-generate a MADR 4.0-compliant skeleton in the item description. Required sections: # ADR-NNN Title, Status field, Date, Context and Problem Statement H2, Decision Drivers H2, Considered Options table H2, Decision Outcome H2, Positive/Negative Consequences H2. This prevents quality 3/10 issues documented in SPIKE-046. Template should be pre-populated with ID, title, date from item metadata.

**Result (2026-04-05):** Implemented in commit fd85394. Created `packages/shared/src/templates/madr.ts` with `generateMadrTemplate()` and `isMadrCandidate()`. Wired into `addItem()` — auto-populates MADR 4.0 skeleton when type=doc, no description provided, and tags include 'adr' or title starts with 'ADR'. Template includes: Status, Date, Context, Decision Drivers, Options table, Decision Outcome, Consequences. Verified via CLI: `devsteps add doc 'ADR-Test' --tags adr` produces correct template."