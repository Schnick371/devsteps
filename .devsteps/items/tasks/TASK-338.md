11 result.json files have fake timestamps (T00:00:00Z). Root cause: agent prompts say 'completed_at: ISO timestamp' but agents fabricate dates instead of using real time. Fix: update MandateResult instructions in coordinator prompt to explicitly say: completed_at MUST be the actual current UTC time at the moment you call write_mandate_result, NOT a hardcoded placeholder. Add a Zod superRefine() check on WriteMandateResultSchema that rejects strings ending in T00:00:00Z.