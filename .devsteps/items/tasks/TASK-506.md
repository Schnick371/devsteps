Handler für `devsteps_docs_assemble`: **Pflicht-Args** `{ root_arch_id: string, max_nodes: number, include_content: boolean }` (keine optionalen Defaults im Schema — per C4-Entscheidung erzwungen). Logic: traversiert BOM-Teilbaum ab `root_arch_id`, sammelt bis zu `max_nodes` Nodes, wenn `include_content: true` → Dateiinhalt je Node via `validateWorkspacePath` laden, bei >8KB pro Node auf 8KB truncaten. Gesamt-Budget-Check: bei >100KB Response `{ truncated: true, nodes_returned: N, nodes_total: M }`.