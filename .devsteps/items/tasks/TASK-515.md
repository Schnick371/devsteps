DOC-001 (copilot-doc-item-access.md) und DOC-052 (vermutlich ähnlicher Content) überschneiden sich inhaltlich.

**Aufgaben:**
1. DOC-001 und DOC-052 aufrufen via `devsteps get DOC-001` und `devsteps get DOC-052`
2. Inhaltlichen Overlap analysieren
3. Entscheidung: Eines als `obsolete` markieren oder Inhalte konsolidieren
4. BOM (`docs-map.json`) anpassen falls einer der Nodes referenziert ist
5. Links (`documents`/`documented-by`) prüfen und ggf. neu setzen