Edit each aspect agent `.agent.md` (web-analyst, internal-analyst, etc.) to:
- Replace prose markdown output instructions with: call `write_analysis_report` MCP tool with structured `AnalysisBriefing` JSON
- Document the schema fields inline so the agent knows what to populate