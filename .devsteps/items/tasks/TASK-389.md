STORY-223 (draft) description explicitly references "reads docs-map.yaml via readDocsMap()" — after STORY-233 migration the file is docs-map.json.

Update STORY-223 description to remove YAML-specific language ("docs-map.yaml" → "docs-map.json").

After updating STORY-223, link it to STORY-233 with relationship "depends-on".