Write an Architectural Decision Record (ADR) documenting the confirmed finding that passive observation of VS Code LM API calls from third-party extensions is NOT possible (vscode.lm.onDidChangeChatModels = registry only; sendRequest() = caller must own call; AITK has no cross-extension observer API). Document: (1) Evidence per API, (2) Decision: TypeScript TraceLogger as active writer in coord agents, (3) TraceEvent schema draft (ring, agent, status, item_id, duration_ms, triage), (4) Future path: OpenTelemetry spans via AITK OTLP collector if coord agents are explicitly instrumented. Save ADR to docs/architecture/adr-001-agent-trace-strategy.md. Affected: docs/architecture/