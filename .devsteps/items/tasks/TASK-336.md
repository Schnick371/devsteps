Add `devsteps cbp cleanup [--dry-run] [--ttl-days <n>]` CLI command. Archival strategy: sprint dirs older than N days where all items are done get moved to .devsteps/cbp/archive/. Default TTL: 30 days. Dry-run mode prints what would be archived without deleting. Implementation: packages/cli/src/commands/. Also add a post-done-gate hook in mcpServerManager.ts to optionally trigger cleanup.