[Update 2026-04-03] Spec conflict resolved: STORY-233 (adjacency list migration) is prerequisite. After STORY-233, shadow must include BOTH position AND parent_arch_id. See TASK-390 for tracking.

Part of the dual-write architecture in STORY-221. The JSON shadow at .devsteps/index/docs-map-positions.json is a flat array with fields: arch_id: string, doc_id: string | null, title: string, position: string (dot-notation e.g. "1.2.3" — implemented by STORY-221), parent_arch_id: string | null (to be added after STORY-233 adjacency list migration, direct copy of parent_id from DocsMapNode), depth: number, devsteps_items: string[]. Two representations are required: position (human-readable, derivable from ordered traversal) and parent_arch_id (direct structural reference). Written atomically by writeDocsMap() alongside docs-map.json (after STORY-233 migration). Enables O(1) cross-reference queries ("which DevSteps items link to BOM level 2 nodes?") without tree traversal. First-time migration: requires one devsteps docs rebuild CLI command (backfill). This task is partially included in STORY-221 scope; created as sub-task for tracking purposes. Prerequisite: STORY-233.SPIKE-041 update: TASK-377 is NOT a prerequisite for STORY-229 (CodeLensProvider) or STORY-230 (HoverProvider). Required only for ARCH tree display and TreeView group-by-arch. Remove dependency from those stories.