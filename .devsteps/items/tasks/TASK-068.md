Create explicit light/dark theme icon variants if needed. VS Code best practice: Use currentColor for automatic theming (preferred). Alternative: Provide separate icons for light/dark themes if design requires. Implementation: Test Activity Bar icon in all themes (Dark+, Light+, High Contrast), evaluate if currentColor sufficient or if explicit variants needed, create devsteps-activitybar-light.svg and devsteps-activitybar-dark.svg if necessary, update package.json with theme-specific references. Acceptance criteria: Icon clearly visible in all themes, passes accessibility contrast ratios, no user complaints about visibility. Note: Only create variants if currentColor approach insufficient (uncommon for simple stroke icons).