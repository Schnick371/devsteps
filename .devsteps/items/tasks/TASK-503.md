Implementiere fail-fast-Verhalten in `devsteps_docs_map_read`: wenn `affected_paths` leer ist, sofort `{ success: false, error: 'affected_paths not set on doc item DOC-NNN — run migrate-doc-affected-paths first' }` zurückgeben. Kein Fallback auf manuelle Suche.Implemented: handler `packages/mcp-server/src/handlers/devsteps_doc_read_content.ts` with 3-tier fallback (mdFile → description → error). Fail-fast when affected_paths is empty. Tool def in `packages/mcp-server/src/tools/docs-map.ts`. Registered in http-server.ts, server.ts, tools/index.ts. Depends on TASK-494 (path-guard). Commit: 23b19dd.