Add pino child-logger with dispatch_id UUID per sprint-run to MCP server. Each new sprint/session gets a fresh UUID4 dispatch_id. All MCP tool calls within that session log with dispatch_id field. Use custom devsteps.* namespace (NOT OpenTelemetry — too heavy until gen_ai.* agent spans stabilize, estimated Q3 2026). Enables correlation of all tool calls within a single coord session.