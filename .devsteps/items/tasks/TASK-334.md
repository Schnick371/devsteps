Update MandateResultSchema in packages/shared/src/schemas/cbp-mandate.ts: (1) item_ids: change from z.array(z.string()) to z.array(z.string()).min(1) — prevents empty item_ids CBP files. (2) analyst: add z.string().regex(/^devsteps-R\\d-/) to enforce standard agent name format. (3) Add optional fields from TASK-326: t3_recommendations, n_aspects_recommended, failed_approaches as z.optional(). These are schema-compatible additions (backward-compatible). Also fix MandateType enum: 'quality-review' vs 'quality' inconsistency.