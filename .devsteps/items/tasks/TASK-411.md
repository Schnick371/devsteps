There is no test asserting that locally-hardcoded type arrays match the Zod enum source of truth. This is precisely why doc is missing from system.ts undetected. Add a test in packages/shared/src/core/validation.test.ts or packages/mcp-server/src/ that asserts all type enum consumers match ItemType.options. Prevents future desync regression.5 freeze tests added in enum-sync.test.ts. Commit 05f2e49.