Add non-blocking runtime version check in packages/extension/src/extension.ts activate() function. If vscode.version &lt; '1.109.0': show a warning message via vscode.window.showWarningMessage() stating that VS Code 1.109.0+ is required for parallel runSubagent dispatch. The warning should be non-blocking (do not throw, do not prevent activation). The engines.vscode field in package.json already enforces ^1.109.0 at install time — this guard is for runtime clarity.