Edit `devsteps-coordinator.agent.md` to:
- Read aspect agent briefings via `read_analysis_envelope` MCP tool
- Write final `CompressedVerdict` JSON via `write_verdict` MCP tool
- Remove prose-based handoff instructions