Write Vitest unit tests in packages/shared/src/utils/trace-logger.test.ts: (1) TraceEvent schema validates correct events and rejects invalid ones, (2) TraceLogger.emit() appends valid JSONL lines to the target file, (3) TraceLogger handles file-not-found by creating directory, (4) TraceLogger rotation: when >500 events, older events are dropped, (5) Async non-blocking: emit() resolves in <5ms on local disk. Use tmp directory fixtures. Mock: none (use real fs in tmp). Affected: packages/shared/src/