packages/shared/src/types/commands.ts TYPE_SHORTCUTS map at lines 14-22 does not include a 'doc' entry. This is the same desync class as BUG-077. Both doc and adr (if added in future) need entries. Fix: add `doc: 'doc'` to TYPE_SHORTCUTS.Fixed: added `doc: 'doc'` to TYPE_SHORTCUTS. Commit 6a70ff3.