getTraceabilityGraphScript() uses JSON.stringify() to inject nodes and edges data into a <script> tag template literal in dashboardPanel.ts. Node.js JSON.stringify() does not escape </script> sequences, creating a latent OWASP A03 injection path. Current practical severity is LOW (node status + edge types use controlled enums — no free-text data in graph model today). However, the pattern is incorrect and must be fixed before Phase B adds output_format: embed-block rendering, which will mix user-supplied title/description text into the same pipeline. Fix: safeJsonStringify helper that replaces < → \u003c and > → \u003e. Also audit burndownChart.ts for same pattern. This fix is a Phase B PREREQUISITE.
## Fix Applied

Created `packages/extension/src/utils/scriptHelpers.ts` with `safeJsonStringify(value: unknown): string` that escapes `<`, `>`, `&` as Unicode sequences (`\u003c`, `\u003e`, `\u0026`).

Applied in:
- `packages/extension/src/webview/renderers/traceabilityRenderer.ts` — `getTraceabilityGraphScript()`: `nodes` and `edges` now via `safeJsonStringify()`
- `packages/extension/src/webview/renderers/burndownRenderer.ts` — `getBurndownChartScript()`: `labels`, `idealData`, `actualData` now via `safeJsonStringify()`

Build: clean. 444 unit tests passed. Merged to main.