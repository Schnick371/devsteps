7 defects in the doc ItemType infrastructure block DOC item creation.

**DEFECT-3 (CRITICAL):** `INDEX_FILENAMES.TYPE` missing `doc: 'docs.json'` → TypeError crash in addItemToIndex  
File: packages/shared/src/types/index-refs.types.ts

**DEFECT-1 (HIGH):** `getTypePrefix` missing `doc: 'DOC'` → wrong counter key (`ITEM` instead of `DOC`)  
File: packages/shared/src/utils/index.ts

**DEFECT-4 (HIGH):** `allTypes` no-filter array missing `'doc'` → DOC items never returned  
File: packages/shared/src/core/list.ts

**DEFECT-5 (HIGH):** Prefix decode map missing `DOC: 'doc'` → DOC items invisible to search  
File: packages/shared/src/core/list.ts

**DEFECT-6 (MEDIUM):** `listArchivedItems` allTypes missing `'doc'`  
File: packages/shared/src/core/list.ts

**DEFECT-7 (LOW):** Counter init stores lowercase `doc: 0` but runtime reads uppercase `DOC`  
File: packages/mcp-server/src/handlers/init.ts

**DEFECT-2 (MIGRATION):** Existing projects missing `items/docs/`, `docs.json`, `DOC: 0` counter. Needs migration script.

Fix order: DEFECT-3 → DEFECT-1 → DEFECT-4+5 → DEFECT-6 → DEFECT-7 → DEFECT-2 (migration last).

## Resolution

All 7 defects fixed in commit `1b41091`.  
Merged to main via `fix(doc-type): merge BUG-075`.  
456 tests pass. Build clean.

**Files changed:**
- `packages/shared/src/types/index-refs.types.ts` — DEFECT-3: added `doc: 'docs.json'`
- `packages/shared/src/utils/index.ts` — DEFECT-1: added `doc: 'DOC'` to `getTypePrefix`  
- `packages/shared/src/core/list.ts` — DEFECT-4,5,6: added `'doc'` to all three allTypes arrays + `DOC: 'doc'` to prefix decode map
- `packages/mcp-server/src/handlers/init.ts` — DEFECT-7: counter keys now uppercase via `getTypePrefix(type)`
- `packages/cli/src/commands/init.ts` — DEFECT-7: same fix