TypeScript reports 5 compile errors in packages/extension/src/mcpServerManager.ts:

**Error group 1 — httpServer.url (lines 244, 248, 262):**
`Object is possibly 'undefined'` on `this.httpServer.url` despite assignment on the line above. Root cause: TypeScript cannot narrow optional class fields (`private httpServer?: {...}`) after assignment because any class method could reset the field asynchronously.

**Error group 2 — vscodeApi lm / McpStdioServerDefinition (lines 297, 319):**
`Property 'lm' does not exist on type 'never'` and `Property 'McpStdioServerDefinition' does not exist on type 'never'`. Root cause: TypeScript `in` narrowing — after `if ('McpHttpServerDefinition' in vscodeApi) { return; }`, TypeScript infers the continuation type as `VsCodeWithMcpApi` minus types that have `McpHttpServerDefinition`, resulting in `never` because the interface declares it as required.

**Affected file:** packages/extension/src/mcpServerManager.ts
**Package:** packages/extension**Gate Review Result (2026-03-08T17:05:00Z):** PASS — verdict GO, confidence 0.99. All 5 TypeScript errors eliminated (TS2532 ×3, TS2339 ×2). Interface split approach (VsCodeStdioMcpApi + VsCodeHttpMcpApi) verified correct. Biome clean, 417/417 tests pass. extension.ts:258 TS2345 confirmed pre-existing and out of scope. MandateResult: .devsteps/cbp/bug-BUG-069/c7f1d3e9-4a2b-4e85-b6c8-9d0f2e1a3b5c.result.json**Resolution (2026-03-08):**
Fixed by splitting `VsCodeWithMcpApi` into `VsCodeStdioMcpApi` (base, stdio API) and `VsCodeHttpMcpApi extends VsCodeStdioMcpApi` (HTTP API, VS Code 1.109+). This prevents TypeScript `in`-operator narrowing from collapsing the stdio fallback type to `never`. Used typed local const for `httpServer` after async assignment to satisfy strict null checks without `!` forbidden non-null assertions.

**Gate:** PASS (0.99 confidence) — 417/417 tests, biome clean, typecheck clean.
**Commits:** 3da4482, 0437814, 8a68c3a on main.