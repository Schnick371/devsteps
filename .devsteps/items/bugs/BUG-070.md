In `packages/extension/src/extension.ts` at line 258, a pre-existing TypeScript error remains:

```
error TS2345: Argument of type 'Uri' is not assignable to parameter of type 'string'.
258   const spiderWatcher = new SpiderEventsWatcher(workspaceRoot, (event) => {
```

`workspaceRoot` is a `vscode.Uri` but `SpiderEventsWatcher` expects a `string`. Fix: use `workspaceRoot.fsPath` instead of `workspaceRoot`.

This was identified as out-of-scope during BUG-069 fix. It prevents full CI typecheck green on the extension package.**Resolution (2026-03-08):** Changed `new SpiderEventsWatcher(workspaceRoot, ...)` to `new SpiderEventsWatcher(workspaceRoot.fsPath, ...)` in extension.ts line 258. `workspaceRoot.fsPath` extracts the filesystem string path from the `vscode.Uri`. Typecheck now fully clean, 417/417 tests pass. Commit: `ce1413c`.