sprint_id field accepts any string via z.string().min(1), including "../../etc/passwd". path.join() normalizes but does NOT confine — join(cbpDir, '../../../etc') resolves to /etc. Fix: add .regex(/^[a-zA-Z0-9_.-]+$/) to sprint_id in WriteMandateResultSchema AND MandateSchema. Also add pattern constraint to all CBP tool inputSchema JSON for sprint_id. All 17 existing sprint IDs (format: YYYY-MM-DD or YYYY-MM-DD-NAME) pass the regex.Fixed in commit 2eb2c1e on branch task/TASK-347-copilot-instructions. Added sprint_id regex /^[a-zA-Z0-9_.-]+$/ to WriteMandateResultSchema and pattern constraint in cbp.ts tool inputSchema.