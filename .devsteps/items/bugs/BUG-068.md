The in-process HTTP MCP server (startHttpMcpServer) is the primary path for all VS Code 1.109+ users but has zero automated tests. Uncovered: CORS handling, body size limits, workspace path injection, concurrent calls, graceful shutdown. BATS tests only cover CLI binary. Vitest only covers shared/cli packages.