CLI integration baseline fuer policy-driven flows stabilisieren und failing paths beheben.Resolved: validation.ts XOR guard added in commit ba3c828 (Implements: BUG-082). blocks+implements coexistence to same target now allowed. All 33 CLI integration tests pass.