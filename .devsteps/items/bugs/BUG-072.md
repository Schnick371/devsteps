The item loader regex in packages/extension/src/treeView/utils/itemLoader.ts does not include the DOC prefix. When a DOC item is created and the extension tries to load it for TreeView display, loadItemWithLinks('DOC-001') returns null silently. Fix as part of Phase 1 (27-location atomic update), but tagged here separately for tracking. See SPIKE-036 for full context.