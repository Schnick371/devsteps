The `doc` ItemType is present in the Zod enum at `packages/shared/src/schemas/index.ts:45` but is absent from `.devsteps/config.json` `item_types` array and `item_prefixes` map. This means `devsteps add --type doc` fails config validation silently. This also blocks ADR-as-doc tracking. Fix: add `doc` to `item_types` array and `doc: 'DOC'` to `item_prefixes`. Affects packages/cli, packages/mcp-server runtime behavior.Fixed: added `doc` to config.json item_types + item_prefixes. Commit 6a70ff3.