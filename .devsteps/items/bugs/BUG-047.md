Extension 0.8.3 (pre-release) calls npx without @next tag, downloads MCP Server 0.6.14 (@latest) instead of 0.8.1-next.5 (@next). Version 0.6.14 is incompatible with refs-style index causing all MCP commands to fail.