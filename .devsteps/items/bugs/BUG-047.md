Already fixed by TASK-207. The `getMcpServerPackage()` in packages/extension/src/mcpServerManager.ts correctly checks `context.extension.packageJSON.preRelease === true` and returns `@schnick371/devsteps-mcp-server@next` for pre-release builds. Verified: no issue in current code.