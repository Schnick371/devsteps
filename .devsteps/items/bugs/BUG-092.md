packages/extension/src/webview/dataProviders/traceabilityProvider.ts loads items via listItems() which returns index entries only — index schema has no linked_items field. As a result linked_items is always undefined, all edge arrays are empty, and the graph renders 50 isolated nodes with zero connections. Fix (Option A): call getItem(id) for each displayed node to load full metadata with linked_items; cache results to avoid redundant reads. Limit to top-50 nodes by title/type sort as already implemented.

Source: analyst-archaeology §6, analysis-impact §7.

## Acceptance criteria
- Traceability graph renders edges between linked items
- getItem() calls are bounded to ≤50 nodes (not all 1000+ items)
- Performance: initial render ≤ 2 seconds for typical project

## Resolution (2026-04-12)
Added `includeLinkedItems` flag to `ListItemsArgs` in shared package. Extension's `loadAllData()` now passes `{ includeLinkedItems: true }`, and traceability provider receives full linked_items data. Graph now renders edges correctly. Commit: 39bbcfb.