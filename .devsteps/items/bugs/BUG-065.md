WriteMandateResultSchema (TASK-334 refactor) removed findings.max(6000) and recommendations.item.max(200) from source. Compiled dist still has old limits causing confusing rejections. Fix: add findings.max(12000) and recommendations items().max(300) to WriteMandateResultSchema.extend(). Update MAX_MANDATE_FINDINGS_CHARS constant to 12000. ReadMandateResultSchema stays lenient (no max) for backward compatibility with historical CBP files.Fixed in commit 2eb2c1e on branch task/TASK-347-copilot-instructions. Added findings.max(12000) and recommendations.item.max(300) to WriteMandateResultSchema.extend(). Updated MAX_MANDATE_FINDINGS_CHARS constant from 6000 to 12000. ReadMandateResultSchema stays lenient for backward compatibility. Updated tool description in cbp.ts.