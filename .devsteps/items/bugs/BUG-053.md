Fixed in packages/shared/src/core/update.ts: used optional chaining `metadata.linked_items?.[relationshipType] ?? []` to guard against old-format items lacking linked_items structure. Also fixed the same pattern in packages/mcp-server/src/handlers/update.ts.