packages/mcp-server/src/tools/system.ts contains a hardcoded ITEM_TYPE_ENUM local array that does NOT match the Zod ItemType enum. It is currently missing 'doc'. This means mcp_devsteps_list with type filter 'doc' silently returns empty results (not an error). Fix: derive ITEM_TYPE_ENUM from `ItemType.options` (Zod 3.x property) instead of hardcoding. Same fix must be applied to inline enum arrays in crud.ts. This eliminates the entire enum desync risk class. HIGH SEVERITY — silent data loss behavior.Fixed: replaced hardcoded ITEM_TYPE_ENUM with ItemType.options from Zod. Commit 6a70ff3.