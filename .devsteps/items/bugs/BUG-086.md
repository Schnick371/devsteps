`devsteps_docs_bom_commit.ts:87,104,~130` schreibt `id: slug` (z.B. `docs-architecture-overview`) statt `ARCH-NNN`-Format in `docs-map.json`. Verletzt ADR-S2-12 explizit. Jeder `bom_commit`-Aufruf produziert korrumpierte BOM-Nodes. Fix: alle 3 `id: slug` Zuweisungen durch `id: nextArchId(devstepsDir)` ersetzen (Risk R2, likelihood 3 × severity 3 = 9).