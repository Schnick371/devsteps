Phase B addition. canonical-for (and its inverse derived-from) were identified as needed in Session 1 (STORY-219 covers broader DOC item type + 4 relation types — NOTE: STORY-219 has overlapping scope; this item specifically covers the TSD-context activation trigger where canonical-for is needed as soon as Phase A docs-map.yaml is live). Overlap with STORY-219: STORY-219 includes canonical-for in its 4-relation schema change. This item tracks the TSD-specific downstream wiring: doc_section metadata bag bridge (Phase A) and the TreeView relation renderer update for the new relation type pair. If STORY-219 ships first, this item may be scoped to TreeView renderer only.

The metadata.doc_section: { doc_id, anchor } metadata bag approach (Phase A bridge) temporarily replaces canonical-for until Phase B. Add canonical-for to FLEXIBLE_RELATIONSHIPS, add inverse pair to INVERSE_RELATIONS in link.ts (TypeScript compile-fail guard), add to LinkedItems Zod object defaults, update extension TreeView relation renderer.