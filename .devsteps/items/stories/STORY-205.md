Submit @schnick371/devsteps-mcp-server to the GitHub MCP Registry so it appears in VS Code @mcp marketplace search. Requires: npm stable release done (STORY-204). Steps: 1) brew install mcp-publisher, 2) cd packages/mcp-server && mcp-publisher init, 3) mcp-publisher login github, 4) mcp-publisher publish. Timeline: ~15-30min after publish to appear in @mcp search. Enable: chat.mcp.gallery.enabled=true in VS Code settings.