New MCP tool that wraps writeDocsMap() from packages/shared/src/core/docs-map.ts. Tool signature: mcp_devsteps_docs_map_write(tree: DocsMapRoot): { success: boolean, arch_ids_written: string[], shadow_path: string }. Atomic .tmp → rename write pattern (same as existing write_sprint_brief). Validates all id fields match ARCH-[A-Z0-9-]+ pattern before write. Rejects duplicate ARCH-NNN values within the manifest (uniqueness invariant). Requires STORY-221 (docs-map.ts module) to be complete first.