VS Code contributes.markdown.previewScripts JavaScript file. GUARD: reads document frontmatter (tsd: true marker) before activating — must NOT activate on non-TSD markdown files. Renders a floating depth slider (1–6 levels) in the markdown preview. On slider change: calls filterHeadingsByLevel(level) which hides/shows heading sections and injects breadcrumbs at odd levels (see TASK for breadcrumb implementation). State persistence: acquireVsCodeApi().setState({ depthLevel: N }) for in-session state (zero round-trip to host). Cross-session state requires VS Code message routing (pending spike — Phase C gate). Phase C. Depends on TASK-375 (manifest entry).