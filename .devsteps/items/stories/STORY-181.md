R5-gate-reviewer Phase 1 and sibling reviewer/quality/test agents hardcode `npm run build`, `npm test`, `npm run lint` — breaking cross-project applicability for Python-only or non-npm workspaces. Replace with language-agnostic guidance (detect and use the appropriate build/test/lint tool); check all agent files in .github/agents/, packages/cli/.github/agents/, packages/mcp-server/.github/agents/ for the same pattern.**Completed 2026-03-04:** Replaced hardcoded `npm run build / npm test / npm run lint` in Phase 1 Automated Gates and Automated Checks FIRST sections across all 6 affected agent files. Full Spider Web dispatch (R1: archaeology + research → R2: constraints + impact → R4: worker-coder ×2 + exec-doc → inline gate validation). All 6 files verified clean (0 npm-specific gate commands remaining), ESCALATE guard added, all files ≤ 150 lines. Merged abc8efa to main.