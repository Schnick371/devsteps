Empirically measure token consumption and latency for two dispatch patterns: (A) Ring-coordinator depth-3 model (coord → ring-coord → leaf) vs (B) flat FULL-tier dispatch (coord → all analysts directly). Capture: total tokens per ring, round-trip latency, synthesis quality score. Goal: determine whether ring-coordinator overhead is offset by per-ring synthesis quality gain. Decision gate for Story B cost model.