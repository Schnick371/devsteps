New MCP tool (CANNOT reuse mcp_devsteps_list — different data source: YAML tree vs JSON index). Tool signature: mcp_devsteps_doc_depth_query(bom_position_prefix?: string, max_depth?: number): { nodes: DocsMapNode[], total_nodes: number }. Reads docs-map.yaml via readDocsMap() from docs-map.ts. Traverses the tree, filtering by bom_position_prefix if provided (e.g., "ARCH-001" returns that node and all children). Returns nodes flattened to max_depth (default: full tree). No coupling to list.ts or any JSON index. Requires STORY-221.