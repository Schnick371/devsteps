Activate the existing `doc` ItemType in config.json to immediately enable ADR tracking as DOC-NNN items. This requires 0 TypeScript changes (doc already exists in the Zod enum). Deliverables: (1) doc in config.json, (2) system.ts enum desync fixed, (3) MADR 4.0 template generated by devsteps add --type doc, (4) ADR-008 created as first DOC item to prove the path. This is the quickwin step before the full Knowledge Subsystem (STORY-145).

Done: doc type activated. validation.ts guards added, config.json allowed/forbidden links, CLI updated, DOC-001 created. Commit 357997b.