Extends mcp_devsteps_get with an optional output_format: 'json' | 'embed-block' | 'markdown-card' parameter (default: 'json'). This makes a SEPARATE mcp_devsteps_doc_embed tool UNNECESSARY (reuse invariant). Create packages/shared/src/core/format-helpers.ts with: htmlEscape(str: string): string, safeJsonStringify(obj: unknown): string (from BUG-073 fix), formatAsEmbedBlock(item: ItemMetadata): string, formatAsMarkdownCard(item: ItemMetadata): string. CRITICAL SECURITY INVARIANT: formatAsEmbedBlock MUST call htmlEscape() on ALL user-supplied fields (title, description, status, tags) before HTML rendering. This is non-negotiable per the CVE-2025-65716 class of vulnerabilities. Phase B. Depends on BUG-073 (security fix must land first).