Refactor the existing VS Code extension WebviewPanel into an SPA-style landing page where every section (Spider Web, Burndown, Eisenhower, Timeline, Traceability, Stats) is represented as a card with a mini-preview SVG. Clicking a card navigates to a dedicated detail tab via hash routing. Acceptance: all existing dashboard functionality preserved, landing page is the default view, tab switching is instant via postMessage + webview.setState(). Affected: packages/extension/src/webview/, packages/extension/media/dashboard.css