At sprint scale (10 analysts x 12K char findings), read_mandate_results returns ~120K chars in one JSON response, risking coord context window overflow. Add optional fields[] parameter: if provided, each MandateResult in the response only includes those fields (e.g. [\"status\",\"confidence\",\"recommendations\"] skips full findings). Coord can do a quick status-check read, then a targeted read for specific analyst findings. Update tool inputSchema and handler logic.